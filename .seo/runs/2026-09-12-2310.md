# Run 2026-09-12-2310 — forced fix (owner instruction)

Owner said: "get rid of the other about and submission links that open up modals (and also remove the modal code) now that we show those things in the footer." Forced `repair` on `/`; no cross-lane vote. Measured cheaply from the previous run's live numbers (nothing else changed on the live site between runs).

## Measured

- Inherited from run 2026-09-12-2220 (live, after 4fe00a5 deployed): Lighthouse mobile `/` perf 88 / a11y 100 / seo 100, FCP 1.6 s, LCP 3.1 s, TBT 270 ms, 249 KiB; zero failing binary audits. `/` loaded jQuery (85.6 KB), vex.min.js (7 KB), vex.css + vex-theme-wireframe.css (17 KB) and `scripts.js` solely to open a dialog that duplicated the `#about` / `#submit` text already rendered on the page.
- GSC, Bing, census, AI panel, radar, SERP, competitors, backlinks, seasonality, outcomes: unchecked (not connected; unchanged since 2026-09-12-2205).
- Truth: 5 rules, 0 contradictions before and after.

## Candidates

Forced by owner. Roadmap Phase 0 item 3 follow-up ("dropping vex + jQuery from `/` is a later repair") was already queued as debt.

## Did

`src/index.html`
- Removed the `<nav>` (about / submit links) entirely.
- The 10 empty TOC cards are now `<div class="card-empty" aria-hidden="true">` instead of anchors to `#submit`.
- `#about` and `#submit` sections moved out of `<main>` into `<footer class="site-notes">` after it.
- Dropped `css/vex.css`, `css/vex-theme-wireframe.css`, `js/jquery.min.js`, `js/vex.min.js`, the inline vex config and `js/scripts.js`. The page now ships zero first-party JavaScript (only JSON-LD and the Spidleweb analytics script remain).

`src/css/main.css`
- Card rules now select `.row a, .row .card-empty` so the decorative cards keep their fill, border, skew and responsive sizing; hover/focus states apply to anchors only.
- Removed all `.nav` / `.nav-link` rules (desktop and mobile).

Deleted from the repo: `src/js/jquery.min.js`, `src/js/vex.min.js`, `src/js/scripts.js`, `src/css/vex.css`, `src/css/vex-theme-wireframe.css` (the `src/js/` directory no longer exists; each trace vendors its own libraries).

Also: `CLAUDE.md` architecture tree updated; `.seo/truth-checks.json` rule paths no longer list `src/js/scripts.js`; `.seo/truth.md`, `attributes.md`, `link-inventory.md`, `roadmap.md`, `content-ledger.md` updated.

### Gates

- truth_check: 5 rules, 0 contradictions, 0 missing. Pass.
- health_diff (local tree, `--local-root src --check-links`): 5 violations, all pre-existing h1-count=0 on the five trace pages; 0 broken, 0 redirecting. Pass for the touched page.
- Rendered check (local http.server on `src`): `/` h1=1, main=1, footer=1, nav=0, `<script>`=2 (JSON-LD + analytics), 9 anchors, 0 href-less; `js/scripts.js` and `css/vex.css` 404 locally and nothing requests them. Desktop and mobile full-page screenshots: cover, TOC with skewed empty cards, then the about / submit footer. Pass.
- Lighthouse local mobile `/`: perf 97 / a11y 100 / best-practices 100 / seo 100, FCP 1.7 s, LCP 2.4 s, TBT 0 ms, CLS 0, 213 KiB, no failing binary audits (`.seo/health/lighthouse-2026-09-12-local-after-nav-removal.json`). Previous local after homepage rebuild: perf 92, 100, 100.
- date_modified: sitemap `lastmod` for `/` already 2026-09-12. Pass.
- Live re-measure after deploy: see "Live verification" below.

### Live verification (after fd52d9f deployed, ~30 s after push)

- Live `/`: nav 0, footer 1, no jquery/vex/scripts.js references. Deleted asset URLs still answer 200 `cf-cache-status: REVALIDATED` from the edge cache; nothing requests them, harmless, will age out.
- Lighthouse live mobile `/`: perf 88 / a11y 100 / best-practices 77 / seo 100, FCP 1.8 s, LCP 2.9 s, TBT 280 ms, CLS 0, 212 KiB (`.seo/health/lighthouse-2026-09-12-live-after-nav-removal.json`). Previous live: perf 88, bp 100, 249 KiB.
- Remaining cost is entirely Cloudflare edge injection, not the repo: `challenge-platform/.../jsd/main.js` 655 ms bootup (all of the TBT) + two deprecation warnings; `static.cloudflareinsights.com/beacon.min.js` ERR_CONNECTION_REFUSED (errors-in-console; also drops best-practices to 77); `email-decode.min.js` obfuscates `editors@ichnos.net` in served HTML. Raised as NY-5.

## Not done

- Five trace pages still have no `<h1>` (roadmap Phase 0 item 3b).
- Six boilerplate meta descriptions (Phase 0 item 4).
- Taren `article:author` points at the co-translator (item 6).
- Philippa Snow essay duplicated 5x in the DOM (item 7).
- `datePublished` / `dateModified` in trace JSON-LD (item 5).
- `src/css/skeleton.css` is still an unreferenced dead file (item 9); left alone, separate concern.
- `package.json` still lists `jquery` and `magnify` as dependencies; rem-plus-rom vendors its own copies so nothing reads them from `node_modules`. Cosmetic; left alone.
- GSC panels blocked on NY-1; Cloudflare AI-bot block NY-3 unanswered.

## Needs you

NY-5 opened: three Cloudflare edge injections (Bot Fight Mode JS detections, Web Analytics beacon, Email Address Obfuscation) are now the only JavaScript and the only Lighthouse failures on `/`; Email Obfuscation hides the submissions address from crawlers. NY-1 and NY-3 remain open.

## Portfolio

7 URLs, unchanged. `/` is the only hub; now 213 KiB and JS-free.

## Spend

None.
