# Run 2026-09-12-2220 — forced fix session (owner instruction)

Owner said: commit and push after every improvement; submissions are open; Spidleweb visit counts never appear in copy; Garett Strickland is the editor; Jason Spidle is the web designer; re-measure; add 404.html; add a real heading and crawlable about/submit; add hrefs; fix crawlable issues; retarget the links; Search Console "appears already added"; scoped-commit the other uncommitted changes. Forced mode: measured cheaply, skipped the cross-lane vote, registered everything.

## Measured

- GSC: not connected to this host (no MCP, no env). Property may exist in Google's UI. Unchecked.
- Bing: not connected. Unchecked.
- Health (live, before this session's code changes, after 17051ce deployed): 7/7 pages 200; homepage no longer references agentation; edge cache still served the old bundle URL as 200 REVALIDATED (harmless).
- Vitals — Speed: live Lighthouse mobile `/` perf 90 / a11y 95 / seo 92, FCP 1.6 s, LCP 2.9 s, TBT 250 ms, CLS 0, 223 KiB (pre-fix baseline: perf 65, LCP 4.7 s, TBT 436 ms, 451 KiB). `.seo/health/lighthouse-2026-09-12-live-after.json`. Remaining live binary fails at that point: errors-in-console (one ERR_CONNECTION_REFUSED, origin unidentified), deprecations (Cloudflare challenge script), landmark-one-main, crawlable-anchors, llms-txt (advisory).
- Vitals — Relevance: homepage 48 words, no h1, about/submit copy JS-only.
- Vitals — CTR: unchecked (no GSC).
- Vitals — Links: 6 redirecting internal links (the trace back-links); 16 href-less anchors on `/`; soft 404 site-wide.
- Truth: 5 rules, 0 contradictions (agentation fixed last run). Ledger contradictions open: repo URL (not public), Taren `article:author`, Crawford bluhz anchor.
- Product: owner answers recorded in brand.md / truth.md (editor, web designer, submissions open, no pageview counts in copy).
- Radar, SERP, Links+, Brand, Competitors, Backlinks±, Seasonality, AI panel, AI referrals, Census, Outcomes: unchecked (no tools; nothing changed since 2026-09-12-2205).

## Candidates

Forced by owner; no vote. Order executed: (1) repair back-links, (2) repair sitemap lastmod, (3) repair homepage h1 / crawlable copy / hrefs / main, (4) repair 404.html. Plus scoped-commit of pre-existing README + workflow deletion.

## Did

| Commit | Change |
|---|---|
| e900777 | deploy: drop GitHub Actions workflow in favour of Cloudflare Pages Git integration (pre-existing owner changes, committed as found) |
| c4134df | six `src/traces/*/index.html`: `../../index.html#contents` → `/#contents` |
| f58bd7c | `src/sitemap.xml`: `lastmod` on all 7 URLs (git publication dates; `/` = 2026-09-12) |
| 7718874 | `src/index.html`: `<main>`, wordmark in `<h1>` with accessible title, `#about` and `#submit` sections with the about text, publisher link, editor, web designer, "Submissions are open", `mailto:editors@ichnos.net`; nav + 10 empty cards get hrefs; JSON-LD description matches meta, publisher `url` added. `src/js/scripts.js`: dialog reads from the sections, anchor fallback without JS. `src/css/main.css`: `.cover-title`, `.notes*`, mobile stack. |
| 4fe00a5 | `src/404.html` (noindex, links `/#contents`) + `.missing*` CSS |

All pushed to origin/main after each commit; Cloudflare Pages deploys from main.

State: brand.md, truth.md (3 contradictions moved to Resolved, 2 new resolved rows), roadmap.md Phase 0 items 1, 2, 2b, 3, 5(part) closed, link-inventory.md graph updated, content-ledger.md 4 rows, needs-you.md NY-2 and NY-4 closed, NY-1 annotated, config.json `git.push_after_commit: true`.

### Gates

- truth_check: 5 rules, 0 contradictions, 0 missing. Pass.
- health_diff (local tree, `--local-root src --check-links`): 5 violations (h1-count=0 on five trace pages, untouched); redirecting-internal-link 6 → 0; broken 0. Pass for touched pages.
- Rendered check: local http.server on `src`: `/` 200, h1=1, main=1, h2 about + submit, href-less anchors 0, body 235 words; `/404.html` renders (screenshot). Desktop + mobile full-page screenshots of `/` render the new sections correctly. Pass.
- Lighthouse local mobile on rebuilt `/`: perf 92 / a11y 100 / seo 100, FCP 2.4 s, LCP 2.9 s, TBT 0 ms, CLS 0, 324 KiB, no failing binary audits. `.seo/health/lighthouse-2026-09-12-local-after-homepage-rebuild.json`. Pass.
- date_modified: sitemap `lastmod` for `/` set to 2026-09-12. Pass. Trace JSON-LD still has no `dateModified` (open roadmap item 5).
- Live 404 status and live Lighthouse after deploy of 7718874/4fe00a5: unchecked at write time (deploy in flight).

## Not done

- Live verification that `/this-does-not-exist/` and `/llms.txt` now return 404 (needs the Pages deploy to finish).
- Live Lighthouse re-run on `/` after the homepage rebuild.
- Five trace pages have no `<h1>` (health_diff). Next `repair` candidate.
- Six boilerplate meta descriptions ("<Title> on ICHNOS"). `refresh`, blocked on judgment not tools.
- Taren `article:author` points at the co-translator. `correct`, S effort.
- Snow page duplicates the essay 5× in the DOM.
- `datePublished` / `dateModified` in trace JSON-LD.
- Redundancy: nav still opens a vex dialog with the same text now rendered below it; dropping vex + jQuery from `/` (~100 KB) is a later `repair`.
- The ERR_CONNECTION_REFUSED console error on live `/`: nothing in the repo points at localhost; re-check after deploy (may have been the cached agentation bundle).
- GSC still unreadable from this host (NY-1). Census, CTR, outcomes, priors all blocked on it.
- llms.txt: decided not to add. It was a symptom (soft 404), not a need.

## Needs you

- NY-1 Search Console: connect an MCP or drop CSV exports in `.seo/gsc/`.
- NY-3 Cloudflare managed AI-bot block (ClaudeBot, GPTBot, Amazonbot, CCBot…): keep / turn off / allow-list. Recommendation: (b) turn it off and keep `Content-Signal: ai-train=no` in the repo robots.txt.

## Portfolio

7 public URLs, 1 hub, 6 traces. Invisible share unknown (no GSC). No create candidates considered.

## Spend

None. Local Lighthouse and curl only.
